<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mvp.neal.com">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mvp.activity.UserInfoActivityMVP"></activity>
        <activity android:name=".mvc.activity.UserInfoActivityMVC" />
        <activity
            android:name=".MainActivity"
            android:label="导航页">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".singleInstance.activity.ActivityB"
            android:label="ActivityB" />
        <activity
            android:name=".singleInstance.activity.ActivityA"
            android:label="ActivityA" />
    </application>

</manifest>